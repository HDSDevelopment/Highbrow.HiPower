using System;
using System.ComponentModel.DataAnnotations;

namespace Highbrow.HiPower.Models
{
    public enum GenderType {Both, Male, Female}

    public enum MaritalStatusType { Both, Single, Married}
}